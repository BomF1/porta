{% extends 'base.html.twig' %}

{% block title %}Výstupy spracovania{% endblock %}

{% block body %}
    <div class="container-fluid py-3">
        <div class="row justify-content-center">
            <div class="col-12 col-md-10 col-lg-8 col-xl-7">
                <div class="d-flex justify-content-between align-items-center mb-3">
                    <h1 class="h4 mb-0">Dostupné výstupy</h1>
                    <div class="d-flex gap-2">
                        <a href="{{ path('upload_file') }}" class="btn btn-outline-secondary">⬅️ Späť na nahrávanie</a>
                        <a href="{{ path(app.request.attributes.get('_route')) }}" class="btn btn-primary">🔄 Obnoviť</a>
                    </div>
                </div>

                {% if files is empty %}
                    <div class="alert alert-info" role="alert">
                        Zatiaľ neexistujú žiadne výstupné súbory. Nahrajte prosím CSV na spracovanie.
                    </div>
                {% else %}
                    <div class="card shadow-sm border-0">
                        <div class="list-group list-group-flush">
                            {% for file in files %}
                                <a class="list-group-item list-group-item-action d-flex justify-content-between align-items-center"
                                   href="/outputs/{{ file.name }}"
                                   target="_blank">
                                    <div>
                                        <div class="fw-semibold d-flex align-items-center gap-2">
                                            {% if file.type == 'valid' %}
                                                <span class="badge bg-success-subtle text-success border border-success-subtle rounded-pill">
                                                    ✅ Validné dáta
                                                </span>
                                            {% else %}
                                                <span class="badge bg-warning-subtle text-warning border border-warning-subtle rounded-pill">
                                                    ⚠️ Chyby/Nevalidné
                                                </span>
                                            {% endif %}
                                        </div>
                                        <div class="small text-muted mt-1">
                                            📄 {{ file.name }}
                                        </div>
                                        <div class="small text-muted">
                                            📏 {{ (file.size / 1024)|round(1) }} KB •
                                            🕒 {{ file.modified|date('d.m.Y H:i') }}
                                        </div>
                                    </div>
                                    <div class="text-nowrap small text-muted d-flex flex-column align-items-end gap-1">
                                        <span>👀 Zobraziť</span>
                                        <a href="/outputs/{{ file.name }}"
                                           download
                                           class="text-decoration-none small"
                                           onclick="event.stopPropagation();">
                                            ⬇️ Stiahnuť
                                        </a>
                                    </div>
                                </a>
                            {% endfor %}
                        </div>
                    </div>

                    {% set validFiles = files|filter(f => f.type == 'valid') %}
                    {% set invalidFiles = files|filter(f => f.type == 'invalid') %}

                    {% if validFiles|length > 0 or invalidFiles|length > 0 %}
                        <div class="row mt-3">
                            <div class="col-md-6">
                                <div class="card border-success-subtle">
                                    <div class="card-body text-center">
                                        <h5 class="text-success">{{ validFiles|length }}</h5>
                                        <small class="text-muted">Súbory s validnými dátami</small>
                                    </div>
                                </div>
                            </div>
                            <div class="col-md-6">
                                <div class="card border-warning-subtle">
                                    <div class="card-body text-center">
                                        <h5 class="text-warning">{{ invalidFiles|length }}</h5>
                                        <small class="text-muted">Súbory s chybami</small>
                                    </div>
                                </div>
                            </div>
                        </div>
                    {% endif %}
                {% endif %}
            </div>
        </div>
    </div>
{% endblock %}
